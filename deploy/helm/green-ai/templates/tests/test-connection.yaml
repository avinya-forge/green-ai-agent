apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "green-ai.fullname" . }}-test-connection"
  labels:
    {{- include "green-ai.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "green-ai.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
