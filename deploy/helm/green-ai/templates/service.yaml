apiVersion: v1
kind: Service
metadata:
  name: {{ include "green-ai.fullname" . }}
  labels:
    {{- include "green-ai.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "green-ai.selectorLabels" . | nindent 4 }}
