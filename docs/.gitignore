# Allow only canonical documentation files — flat structure (no subdirectories)
# All other files in docs/ are rejected

*

!.gitignore

# Flat canonical docs (Markdown)
!roadmap.md
!backlog.md
!standards.md
!architecture.md
!release.md

# Flat API contracts
!swagger.yaml
!mock_data.json
