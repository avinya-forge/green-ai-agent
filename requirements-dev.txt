# Development-only dependencies for Green-AI Agent.
# Install with: pip install -r requirements.txt -r requirements-dev.txt

-r requirements.txt

# Test framework + coverage
pytest>=7.0,<9.0
pytest-cov>=4.0,<6.0

# Linting
flake8>=6.0,<8.0

# E2E browser automation (used only by tests/test_dashboard_xss_playwright.py)
playwright==1.58.0
